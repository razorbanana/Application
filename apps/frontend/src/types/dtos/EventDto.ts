export type EventDto = {
    id: string;
    name: string;
    description: string
    location: string
    event_date: string
    capacity: number
    visitorCount: number
    public: boolean
    isJoined: boolean
}